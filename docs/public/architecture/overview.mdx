---
title: "Architecture Overview"
description: "System components and data flow in Claude-Mem"
---

# Architecture Overview

## System Components

Claude-Memu operates as a Claude Code plugin with these core components:

1. **Plugin Hooks** - Capture lifecycle events via bun-runner + worker-service subcommands
2. **Smart Install** - Cached dependency checker (runs as first SessionStart hook)
3. **Worker Service** - HTTP API on port 37777, processes observations
4. **UnifiedStore** - Auto-selects between LocalStore (file-based JSON) and MemuStore (memU API)
5. **MCP Tools** - Search tools for querying project history
6. **Viewer UI** - Web-based real-time memory stream visualization

## Technology Stack

| Layer                  | Technology                                |
|------------------------|-------------------------------------------|
| **Language**           | TypeScript (ES2022, ESNext modules)       |
| **Runtime**            | Node.js 18+                               |
| **Database**           | SQLite 3 with bun:sqlite driver           |
| **Vector Store**       | ChromaDB (optional, for semantic search)  |
| **HTTP Server**        | Express.js 4.18                           |
| **Real-time**          | Server-Sent Events (SSE)                  |
| **UI Framework**       | React + TypeScript                        |
| **AI SDK**             | @anthropic-ai/claude-agent-sdk            |
| **Build Tool**         | esbuild (bundles TypeScript)              |
| **Process Manager**    | Bun                                       |
| **Testing**            | Node.js built-in test runner              |

## Data Flow

### Memory Pipeline
```
Hook (stdin) → Worker Service → UnifiedStore → Next Session Hook
```

1. **Input**: Claude Code sends tool execution data via stdin to hooks
2. **Processing**: Hooks invoke worker-service.cjs subcommands via bun-runner.js
3. **Storage**: Worker service stores observations via UnifiedStore (LocalStore or MemuStore)
4. **Retrieval**: Next session's context hook retrieves context from UnifiedStore

### Search Pipeline
```
User Query → MCP Tools Invoked → HTTP API → SessionSearch Service → FTS5 Database → Search Results → Claude
```

1. **User Query**: User asks naturally: "What bugs did we fix?"
2. **MCP Tools Invoked**: Claude recognizes intent and invokes MCP search tools
3. **HTTP API**: MCP tools call HTTP endpoint (e.g., `/api/search/observations`)
4. **SessionSearch**: Worker service queries FTS5 virtual tables
5. **Format**: Results formatted and returned via MCP
6. **Return**: Claude presents formatted results to user

Uses 3-layer progressive disclosure: search → timeline → get_observations

## Session Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│ 0. Smart Install Pre-Hook Fires                                 │
│    Checks dependencies (cached), only runs on version changes   │
│    Not a lifecycle hook - runs before context-hook starts       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 1. Session Starts → Context Hook Fires                          │
│    Starts Bun worker if needed, injects context from previous   │
│    sessions (configurable observation count)                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. User Types Prompt → UserPromptSubmit Hook Fires              │
│    Creates session in database, saves raw user prompt for FTS5  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Claude Uses Tools → PostToolUse Hook Fires (100+ times)      │
│    Captures tool executions, sends to worker for AI compression │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 4. Worker Processes → Claude Agent SDK Analyzes                 │
│    Extracts structured learnings via iterative AI processing    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 5. Claude Stops → Summary Hook Fires                            │
│    Generates final summary with request, completions, learnings │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 6. Session Ends → Worker handles cleanup internally             │
│    No SessionEnd hook registered. Worker marks sessions         │
│    complete when Stop hook fires.                               │
└─────────────────────────────────────────────────────────────────┘
```

## Directory Structure

```
claude-memu/
├── src/
│   ├── cli/                    # CLI handlers
│   │   ├── adapters/           # Platform adapters (claude-code, cursor, raw)
│   │   └── handlers/           # Event type handlers
│   │
│   ├── services/
│   │   ├── memu/               # Storage layer
│   │   │   ├── UnifiedStore.ts # Auto-selecting storage wrapper
│   │   │   ├── LocalStore.ts   # File-based JSON storage
│   │   │   ├── MemuStore.ts    # memU API storage
│   │   │   ├── memu-client.ts  # memU HTTP client
│   │   │   ├── types.ts        # Type definitions
│   │   │   └── index.ts        # Module exports
│   │   ├── worker/             # Worker service
│   │   ├── domain/             # Domain logic
│   │   └── infrastructure/     # Infrastructure services
│   │
│   ├── shared/                 # Shared utilities
│   │   └── SettingsDefaultsManager.ts  # Configuration defaults
│   │
│   └── utils/
│       └── logger.ts
│
├── scripts/                    # Build and utility scripts
│
├── plugin/                     # Plugin distribution
│   ├── .claude-plugin/
│   │   └── plugin.json
│   ├── hooks/
│   │   └── hooks.json          # 5 hook events (Setup, SessionStart, etc.)
│   ├── scripts/                # Built executables
│   │   ├── setup.sh            # Setup hook
│   │   ├── smart-install.js    # Cached dependency checker
│   │   ├── bun-runner.js       # Bun runtime wrapper
│   │   ├── worker-service.cjs  # Worker service (handles all hook events)
│   │   └── mcp-server.cjs      # MCP search server
│   │
│   └── ui/                     # Built viewer UI
│       └── viewer.html         # Self-contained bundle
│
├── tests/                      # Test suite
└── docs/                       # Documentation
```

## Component Details

### 1. Plugin Hooks
All hooks use `bun-runner.js` → `worker-service.cjs` subcommand pattern:
- **Setup** - Runs `setup.sh` for initial setup
- **SessionStart** - smart-install + worker start + context injection + user-message
- **UserPromptSubmit** - worker start + session-init
- **PostToolUse** - worker start + observation capture
- **Stop** - worker start + session summarize

See [Plugin Hooks](/architecture/hooks) for detailed hook documentation.

### 2. Worker Service
Express.js HTTP server on port 37777 (configurable) with:
- Data retrieval HTTP API endpoints
- Viewer UI HTTP/SSE endpoints
- Async observation processing
- Real-time updates via Server-Sent Events
- Auto-managed by Bun

See [Worker Service](/architecture/worker-service) for HTTP API and endpoints.

### 3. Storage Layer
**UnifiedStore** auto-selects between:
- **LocalStore** - File-based JSON storage in `~/.claude-memu/data/` (no API key required)
- **MemuStore** - memU API with semantic search and proactive context

### 4. MCP Tools
MCP-based search tools for querying project history, automatically available in Claude Code sessions.

### 5. Viewer UI
React + TypeScript web interface at http://localhost:37777 featuring:
- Real-time memory stream via Server-Sent Events
- Infinite scroll pagination with automatic deduplication
- Project filtering and settings persistence
- GPU-accelerated animations
- Self-contained HTML bundle (viewer.html)

Built with esbuild into a single file deployment.
